# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/
# database
db/*.db
db/*.db-journal

# allow example env
!.env.example

# local screenshots & artifacts
download/

# deploy secrets & runtime
deploy/.env
deploy/certs/
deploy/runtime/
pvnetwork-repo/
upload/
tool-results/
agent-ctx/

# secrets / server credentials — هرگز به ریپو نروند
scripts/ssh_run.py
worklog.md
.zscripts/
.claude/

# deployment artifacts — build locally, upload via SFTP (never commit)
deploy/panel-bundle/
deploy/panel-bundle.tar.gz
scripts/.env.local
*.tar.gz
